#include <iostream>

/**
 * seção: default initialization
 * compilação no windows: g++ _05-snippet.cpp -o _05-snippet.exe
 * referência: https://www.learncpp.com/cpp-tutorial/variable-assignment-and-initialization/
 */

/**
 * quando nenhum inicializador é fornecido (como no exemplo da variável 'a' do snippet anterior), isto é
 * chamado de inicialização-padrão. em muitos casos, a inicialização-padrão não executa inicialização, e
 * deixa a variável com um valor indeterminado (um valor que não é previsível, e muitas vezes chamado de
 * 'valor lixo');
 *
 * nós iremos discutir esse caso posteriormente na aula. 1.6 - Uninitialized variables and undefined behavior;
 */
int main()
{
    return 0;
}
