#include <iostream>

/**
 * seção: instantiation
 * compilação no windows: g++ _13-snippet.cpp -o _13-snippet.exe
 * referência: https://www.learncpp.com/cpp-tutorial/variable-assignment-and-initialization/
 */

/**
 * o termo instanciação é uma palavra chique que significa que uma variável foi criada (alocada) e
 * inicializada (e isto inclui a inicialização-padrão). um objeto instanciado é muitas vezes chamado
 * uma instância. mais frequentemente, este termo é aplicado a objetos de tipos classe, mas é, geralmente
 * aplicado a objetos de outros tipos também;
 */
int main()
{

    return 0;
}
